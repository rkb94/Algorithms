#include <iostream>
#include <deque>

using namespace std;

int main() {
    deque<int> deq;
    int count = 0;
    int N, M;
    cin >> N;
    cin >> M;
    for (int i = 1; i <= N; ++i) {
        deq.push_back(i);
    }

    for (int j = 0; j < M; ++j) {
        int now;
        cin >> now;
        int size = deq.size();
        int index = 1;

        for (int i = 0; i < ; ++i) {
            
        }
    }

    return 0;
}